{
  "name": "proschool360-chatbot",
  "version": "1.0.0",
  "description": "ProSchool360 Chatbot API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:full": "chromadb run --path ./chroma_db --host 0.0.0.0 --port 8000 & sleep 5 && node server.js",
    "dev": "nodemon server.js",
    "build": "node build_chromadb_node.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "chromadb": "^1.7.3",
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}